<header class="navbar navbar-fixed-top navbar-inverse" style="height: 59px; box-shadow: 2px 5px 10px #888888;padding-left: 2%; padding-right: 2%;">

	<%= link_to "sample app", root_path, id: "logo" %>
		<nav>
			<ul class="nav navbar-nav navbar-right" style="background-color: white;">
				<li><%= link_to "Search", root_path %></li>
				<% if user_signed_in? %>
					<li class="dropdown">
					  <div class="dropbtn" style="margin-top: 5px;"><%=current_user.fullname%></div>
					  <div class="dropdown-content" style="position: fixed;right: 10px;">
					 	<% @profile = current_user.profile%>
					  	<%if @profile.present?%>
					  		<% if @profile.id.present?%>
					  			<%= link_to "Your profile", profile_path(@profile.id) %>
					  		<%end%>
					  	<%else%>
					  		<%= link_to "Update Your profile", new_profile_path %>
					  	<%end%>

					  	<%= link_to "Your test", new_subject_path %>
					    <%= link_to "Log out", destroy_user_session_path, method: :delete %>
					  </div>
					</li>
		
				<% else %>
					<li class="dropbtn account" style="parding: 0;padding: 0;height: 40px;margin-top: 5px;"><%= link_to "Log in", new_user_session_path, style: "
    margin-top: -6px; color: black;" %></li>
				<% end %>
			</ul>
		</nav>
		
</header>